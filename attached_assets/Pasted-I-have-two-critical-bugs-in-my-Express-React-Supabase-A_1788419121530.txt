I have two critical bugs in my Express + React + Supabase Auth + Drizzle ORM monorepo (built with esbuild) that need to be fixed together, since they may be related.

Bug 1: User login/session not persisting.
This is a known issue related to Express not trusting the proxy layer (likely Replit's or Google's reverse proxy in production). Symptoms: user logs in, but the session doesn't persist — they get logged out or session cookie isn't recognized on subsequent requests.

Please check and fix:

Whether app.set('trust proxy', 1) (or the correct proxy count) is set on the Express app — this is required when running behind any reverse proxy for secure cookies to work correctly
Session cookie config: confirm secure: true, sameSite setting (likely needs 'none' if frontend/backend are on different domains, or 'lax' if same-site), and httpOnly: true
Confirm Supabase Auth session/token handling is correctly reading/writing cookies given the above proxy settings
Check if HTTPS is actually being detected correctly by Express in this environment (proxies can make req.secure return false incorrectly if trust proxy isn't set)

Bug 2: Widget install snippet not working on client stores.
The JavaScript snippet generated by our client portal (meant to be pasted into a Shopify store) is not functioning correctly anymore.

Please check and fix:

Whether the snippet is correctly fetching/loading our widget script from the right hosted URL (check for broken paths, incorrect domain references, or CORS issues blocking the script from loading on external domains)
CORS headers on our backend — confirm we're allowing requests from external store domains, not just localhost/our own domain
Whether recent changes to session/cookie handling (Bug 1) broke the snippet's ability to communicate with our backend (e.g., if the snippet relies on cookies that now aren't being set correctly cross-domain)
Test the snippet end-to-end on an actual external test page, not just our own dev environment, to confirm it loads and triggers correctly

Please:

Diagnose both issues with actual logs/error output before making changes — don't guess
Explain the root cause you find for each bug before fixing
Fix both together, since Bug 2 may be a downstream symptom of Bug 1's cookie/session settings
After fixing, test the full flow: user login → session persists → widget snippet loads correctly on an external test page → triggers fire correctly